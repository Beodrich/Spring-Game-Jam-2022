using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
 /*   float copespeedX = 0f;
    float copespeedY = 0f;
     Vector2 VectorFromAngle (float Angolos) {
     return new Vector2 (Mathf.Cos(Angolos), Mathf.Sin(Angolos)); 
 }
    
    // Start is called before the first frame update
    void awake()
    {
        copespeedX = +1f;
        copespeedY = +1f;
        copespeedX = -1f;
        copespeedY = -1f;

        Vector2 MovDir = new Vector2(copespeedX, copespeedY).normalized;
        float movespeed = 6f; 
        float theta = 
        transform.Translate (MovDir *  movespeed);
        transform.translate (VectorFromAngle (Angolos));

    } */

    // Update is called once per frame
    /*
    void Update()
    {
        
    } */

    
}
